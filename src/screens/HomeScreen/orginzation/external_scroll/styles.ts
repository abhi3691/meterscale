import {StyleSheet} from 'react-native';
import ScreenRatio from '../../../../components/constants/ScreenRatio';

const styles = StyleSheet.create({
  header: {
    height: ScreenRatio.height / 2.75,
  },
});

export default styles;
